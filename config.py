# Server Configuration
SERVER_HOST = "0.0.0.0"  # Listen on all network interfaces
SERVER_PORT = 8081       # The port your locks are configured to connect to

# Security
SECRET_KEY = "your_secret_key"  # Replace with the secret key configured in your locks

# Logging
LOG_LEVEL = "INFO"
LOG_FILE = "bike_locks.log" 